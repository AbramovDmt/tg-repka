# TESTING.md — Руководство тестировщика
## Проект: A-frame домик — Telegram Mini App

---

## 1. Что это и как устроено

Telegram Mini App (TMA) для бронирования A-frame домика в Дмитровском районе (Подмосковье).

**Стек:** чистый HTML/CSS/JS — никакого сборщика, никаких зависимостей npm.

```
tg-app/
├── index.html          # Точка входа
├── css/styles.css      # Все стили
└── js/
    ├── data.js         # Весь контент: цены, тексты, даты
    └── app.js          # Логика: роутер, экраны, Telegram SDK

netlify/functions/
└── webhook.js          # Serverless: Telegram-бот webhook

netlify.toml            # Конфигурация Netlify
.env.example            # Шаблон переменных окружения
```

**Production URL:** https://repka-domik.netlify.app  
**Telegram-бот:** @repka_domik_bot

---

## 2. Локальный запуск (без Telegram)

Сборка не нужна. Достаточно открыть файл в браузере.

```
# Вариант А: просто открыть файл
Откройте tg-app/index.html в Chrome/Firefox/Edge

# Вариант Б: через локальный сервер (рекомендуется — clipboard API требует HTTPS/localhost)
cd tg-app
python -m http.server 8080
# затем открыть http://localhost:8080
```

Telegram SDK (`telegram-web-app.js`) подключается из CDN — нужен интернет. Без реального Telegram-клиента SDK отдаёт `null`:
- `MainButton`, `BackButton`, `haptic` не вызываются
- Внешние ссылки открываются через `window.open()`
- Тема принудительно светлая (light)
- Имя пользователя в онбординге не показывается

---

## 3. Переменные окружения (только для webhook)

Скопируйте `.env.example` в `.env` и заполните:

```
cp .env.example .env
```

| Переменная           | Где взять             | Используется в                 |
|----------------------|-----------------------|--------------------------------|
| `TELEGRAM_BOT_TOKEN` | @BotFather → `/token` | `netlify/functions/webhook.js` |

Для тестирования бота локально нужен [Netlify CLI](https://docs.netlify.com/cli/get-started/):
```
npm install -g netlify-cli
netlify dev
```

---

## 4. Состояние приложения (localStorage / sessionStorage)

| Ключ                  | Хранилище      | Значение                              |
|-----------------------|----------------|---------------------------------------|
| `repka_onboarded`     | localStorage   | `'1'` — пользователь прошёл онбординг |
| `repka_offer_shown`   | localStorage   | `'1'` — модалка скидки уже показана   |
| `repka_pending_route` | sessionStorage | ID экрана из deep-link до онбординга  |

**Сбросить всё:** DevTools → Application → Storage → Clear site data.

---

## 5. Карта экранов

```
onboarding (только первый запуск)
    └─ [Начать] ──► home
                        ├─ [info card / кнопка] ──► house
                        │                               └─► booking
                        └─ (оффер-модалка, 500 мс после startApp)

Таб-бар (фиксированный снизу):
  🏠 home   →  home
  📅 book   →  booking
  🔥 sauna  →  sauna
  ⊞  more   →  more
                 ├── bikes
                 ├── nearby
                 ├── instructions
                 └── contact

Флоу бронирования домика:
  booking ──[submit]──► upsellSauna ──► upsellBikes ──► success
                            │                │
                       (или skip)       (или bikes)

Флоу бронирования бани (standalone):
  sauna ──[submit]──► upsellBikes ──► success

Флоу добавления бани к уже выбранным датам домика:
  sauna (per-day mode) ──[Добавить к брони]──► booking ──[submit]──► upsellBikes ──► success
```

**Таб-бар скрыт** на экранах вне основной навигации: `house`, `bikes`, `nearby`, `instructions`, `contact`, `upsellSauna`, `upsellBikes`, `success`.

---

## 6. Тест-кейсы

### ТК-01: Онбординг (первый запуск)

**Условие:** localStorage пуст (Clear site data).

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть index.html | Экран онбординга: 🏕️, приветствие «Привет! 👋», три строки с ✓, кнопка «Начать» |
| 2 | Проверить таб-бар | Таб-бар **не виден** |
| 3 | Нажать «Начать» | Переход на `home`, в localStorage появляется `repka_onboarded = '1'` |
| 4 | Подождать 500 мс | Появляется оффер-модалка «🎁 Скидка 10%» |
| 5 | Повторно открыть | Онбординг не показывается — сразу `home` |

---

### ТК-02: Таб-бар

**Условие:** `repka_onboarded = '1'`.

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Нажать «📅 Бронировать» | Экран `booking`, таб «📅» активен |
| 2 | Нажать «🔥 Баня» | Экран `sauna`, таб «🔥» активен |
| 3 | Нажать «⊞ Ещё» | Экран `more`, таб «⊞» активен |
| 4 | Нажать «🏠 Домик» | Экран `home`, таб «🏠» активен |
| 5 | Открыть `bikes` из `more` | Таб «⊞ Ещё» остаётся активным |
| 6 | Переключить таб во время глубокой навигации | Стек роутера сбрасывается, кнопки «←» нет |

---

### ТК-03: Главная (home)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Просмотреть hero-блок | Текст: «Дмитровский район · 82 км от Москвы», «A-frame домик», «Сосновый лес · Баня · Канал» |
| 2 | Проверить info-карточку | Цена: **8 000 ₽/ночь** · Гости: до 6 · Площадь: 48 м² · 2 этажа · Заезд с 16:00 / до 12:00 |
| 3 | Нажать на info-карточку | Переход на экран `house` |
| 4 | Нажать кнопку «Забронировать» | Переход на экран `booking` |
| 5 | Нажать «🔗 Поделиться с другом» | В браузере: `navigator.share` или `clipboard.writeText` |

---

### ТК-04: Домик (house)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Шапка «Домик», кнопка «←» в шапке |
| 2 | Галерея | 5 слайдов: Фасад, Интерьер, Терраса, Баня, Вечер (градиентные заглушки). Точки-индикаторы |
| 3 | Свайп по галерее | Активная точка переключается |
| 4 | Блок «Что включено» | 11 pill-чипов: Мангал+казан, Wi-Fi, Бельё и полотенца, Кухня, Тёплый пол, Дрова, Парковка 2 авто, Smart TV, Канал рядом, Питомцы ОК, SUP-борд |
| 5 | Блок «Заезд и выезд» | 🔑 Заезд с 16:00, 🚪 Выезд до 12:00 |
| 6 | Блок «Правила» | 7 правил, включая: тишина после 23:00, залог 5 000 ₽ |

---

### ТК-05: Бронирование — Календарь и расчёт

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Шапка «Бронирование», календарь текущего месяца |
| 2 | Прошедшие даты | Серые, не кликабельные |
| 3 | Занятые даты | Красным: `2026-05-23`, `2026-05-24`, `2026-05-25` и другие из `APP_DATA.unavailableDates` |
| 4 | Нажать свободную дату | Подсвечивается (check-in). Блок «Заезд → Выезд» появляется под календарём |
| 5 | Нажать вторую свободную дату позже | Диапазон выделяется. Появляется price block |
| 6 | Расчёт цены (2 будних ночи) | 2 × 8 000 = **16 000 ₽** |
| 7 | Расчёт цены (2 ночи, 5 гостей) | 2 × 8 000 + 2 × 3 000 (баня) = **22 000 ₽** |
| 8 | Нажать дату до первой | check-in переносится на кликнутую, check-out сбрасывается |
| 9 | Диапазон через занятую дату | check-in переносится на кликнутую дату, check-out сбрасывается |
| 10 | Кнопка «‹» в текущем месяце | Невидима — нельзя зайти раньше текущего месяца |
| 11 | Кнопка «›» | Переключает на следующий месяц |
| 12 | Гости «−» / «+» | Диапазон 1–6. При 5–6: появляется подсказка «🛁 При 5–6 гостях баня включена в стоимость» |
| 13 | Гости 5+, выбраны даты | В price block появляется строка «🛁 Баня · 3 000 ₽/сутки», итог пересчитывается |
| 14 | Кнопка «Отправить заявку» | **Disabled** до выбора обеих дат. С датами — показывает итоговую цену |

---

### ТК-06: Флоу бронирования домика (конец в конец)

**Условие:** выбраны даты, 2 гостя.

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Нажать «Отправить заявку · N ₽» | Переход на экран `upsellSauna` («К поездке»). Шаг 1 из 2 |
| 2 | Экран upsellSauna (гости ≤ 4) | «Хотите добавить баню?», цена N × 3 000 ₽/сутки, кнопки «🛁 Добавить баню» и «Нет, спасибо» |
| 3 | Нажать «Нет, спасибо» | Переход на `upsellBikes`. Шаг 2 из 2 |
| 4 | Нажать «Добавить баню» (альтернатива) | Переход на `upsellBikes` с баней в заказе |
| 5 | Экран upsellBikes | Карточка «🚴 Велосипеды», price block с итогом заказа, кнопка «Отправить заявку · N ₽» |
| 6 | Нажать карточку «Велосипеды» | Переход на экран `bikes` |
| 7 | Нажать «Отправить заявку · N ₽» на upsellBikes | Переход на `success`, тип `booking` |
| 8 | Экран success | 🏠 «Заявка отправлена!», order card с деталями брони, «В ближайший час хозяин свяжется с вами» |
| 9 | Нажать «На главную» | Стек сброшен, `home` |

**Флоу при 5–6 гостях:**

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Выбрать 5 гостей, выбрать даты, отправить | Переход на `upsellSauna` |
| 2 | Экран upsellSauna (гости > 4) | «Баня включена на весь период», «Для 5 гостей нужна баня — там +2 места», кнопка «Всё верно, далее →» |
| 3 | Нажать «Всё верно, далее →» | Переход на `upsellBikes` |

---

### ТК-07: Баня с предварительно выбранными датами домика

**Условие:** в `booking` выбраны даты (не нажимая «Отправить»).

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Перейти на вкладку «🔥 Баня» | Карточка «К вашей брони домика», дата — выезд, N ночей |
| 2 | Блок цены | «🛁 Баня · весь период» / «3 000 ₽/сутки × N ночей» / итог справа |
| 3 | Поле «Комментарий» | Textarea для вопросов гостя |
| 4 | Нажать «Добавить к брони · N ₽» | Переход на экран `booking`. В price block появляется строка «🛁 Баня ✕ убрать» |
| 5 | Нажать «✕ убрать» в booking | Строка бани исчезает, итог пересчитывается |
| 6 | Нажать «Нужна только баня без домика — выбрать по часам →» | Экран переключается в почасовой режим (date strip + слоты) |
| 7 | Смена дат в booking | При возврате в баню — снова показывается per-day режим (standaloneOverride сброшен) |

---

### ТК-08: Баня — почасовой режим

**Условие:** даты домика **не** выбраны, или нажата ссылка «выбрать по часам».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Hero: «1 500 ₽/час · до 6 чел. · мин. 2 часа» |
| 2 | Полоса дат (14 дней вперёд) | Первая ячейка: «Сег» + дата + месяц |
| 3 | Дата `2026-05-23` (все слоты заняты) | Ячейка `fully-booked` |
| 4 | Выбрать дату `2026-05-27` | Заголовок слотов обновляется. Слоты `16:00` и `18:00` — серые (booked) |
| 5 | Нажать свободный слот | Слот выделяется. Появляется summary: дата, время–до, цена |
| 6 | Переключить длительность 2→3→4 ч | Цена: 3 000 / 4 500 / 6 000 ₽. Время «до» обновляется |
| 7 | Слот 20:00 + 3 ч | «до 22:00» — не уходит за 22:00 |
| 8 | Кнопка «Забронировать баню» | Disabled до выбора даты и слота. С выбором — показывает цену |
| 9 | Нажать «Забронировать баню · N ₽» | Переход на `upsellBikes`. Флоу аналогичен ТК-06 (шаги 5–9) |

---

### ТК-09: Велосипеды (bikes)

Доступ: «⊞ Ещё» → «🚴 Велосипеды».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Шапка «Велосипеды», hero: «4 велосипеда · 1 500 ₽/день» |
| 2 | Счётчик | По умолчанию: 1. Диапазон: 1–4 |
| 3 | Нажать «+» до 4, ещё «+» | Остаётся на 4 |
| 4 | Проверить сумму | 1 → 1 500 ₽, 2 → 3 000 ₽, 4 → 6 000 ₽ |
| 5 | Блок «Популярные маршруты» | 3 маршрута: Вдоль канала (12 км · 1 ч), До Дмитрова (15 км · 1.5 ч), Яхрома–Дубна (30 км · 3 ч) |
| 6 | Примечание | «Вернуть велосипеды до 20:00. Выдаёт хозяин при заезде.» |
| 7 | Поле «Комментарий» | Textarea с плейсхолдером «Нужен ли детский велосипед, шлем, есть вопросы…» |
| 8 | Кнопка «Забронировать · N ₽» | Переход на `success`, тип `bikes` |

**При входе через upsellBikes из флоу бронирования:**

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Нажать карточку «Велосипеды» на upsellBikes | Экран bikes с накопленным заказом (домик ± баня + велосипеды) |
| 2 | Price block | Строки: 🏠 Домик, 🛁 Баня (если добавлена), 🚴 Велосипеды, Итого |
| 3 | Кнопка «Завершить бронирование · N ₽» | Переход на `success`, тип `booking` |

---

### ТК-10: Что рядом (nearby)

Доступ: «⊞ Ещё» → «🗺 Что рядом».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Карта-заглушка с «A-frame домик», координаты «56.3542, 37.5128» |
| 2 | «Открыть в Яндекс.Картах» | Открывается `https://yandex.ru/maps/?pt=37.5128,56.3542&z=14` |
| 3 | Вкладки | 4 вкладки: Природа, Активности, Места, Дмитров |
| 4 | «Природа» → «Канал им. Москвы» | 300 м · 4 мин пешком · кнопка «›» |
| 5 | Нажать «›» у места с картой | Открывается Яндекс.Карты с поиском |
| 6 | «Берёзовая роща» | Нет кнопки «›» (поле `mapsUrl: ''`) |

---

### ТК-11: Инструкции (instructions)

Доступ: «⊞ Ещё» → «📋 Инструкции».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | 7 пунктов аккордеона: 🗺 Как добраться, 🔑 Заселение, 📶 Wi-Fi, 🔥 Баня, 🍖 Мангал, 📋 Правила, 📞 Экстренные |
| 2 | Нажать заголовок | Содержимое раскрывается, стрелка поворачивается |
| 3 | Нажать другой заголовок | Предыдущий закрывается, новый открывается |
| 4 | Нажать тот же заголовок | Закрывается |
| 5 | Wi-Fi → «Скопировать» | Пароль `nature2024` в буфер, кнопка меняется на «Скопировано ✓» (2 сек) |
| 6 | Как добраться → ссылка на карту | `data-action="openLink"` → Яндекс.Карты |

---

### ТК-12: Контакты (contact)

Доступ: «⊞ Ещё» → «✉️ Связаться с хозяином».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Аватар «С», имя «Сергей», роль «Хозяин домика» |
| 2 | «✉️ Написать в Telegram» | Открывается `https://t.me/aframe_dom` |
| 3 | «📞 Позвонить» | Открывается `tel:+79261234567` |
| 4 | FAQ | 5 вопросов в аккордеоне |
| 5 | «Баня входит в стоимость?» | «Нет, баня бронируется отдельно. 1 500 ₽/час, минимум 2 часа.» |

---

### ТК-13: Навигация и кнопка «Назад»

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | home → house → booking | Кнопка «←» в шапке каждого дочернего экрана |
| 2 | «←» на booking | Возврат на house |
| 3 | «←» на house | Возврат на home, кнопка «←» исчезает |
| 4 | Переключить таб | Стек сбрасывается, «←» исчезает |
| 5 | success → «На главную» | Стек сброшен, `home` |

---

### ТК-14: Оффер-модалка

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Первый запуск → «Начать» → 500 мс | Модалка «🎁 Скидка 10%» |
| 2 | «Получить скидку 10%» | Показывается промокод **FIRST10** с кнопкой «Скопировать промокод» |
| 3 | «Скопировать промокод» | FIRST10 в буфер, кнопка → «Скопировано ✓», через 1.4 с модалка закрывается |
| 4 | «Пропустить» | Модалка закрывается, `repka_offer_shown = '1'` |
| 5 | Повторное открытие | Модалка **не появляется** |

---

### ТК-15: Deep-link (startapp-параметр)

| # | URL / параметр | Ожидаемый экран |
|---|----------------|-----------------|
| 1 | `?startapp=booking` или `?startapp=book` | `booking` |
| 2 | `?startapp=sauna` или `?startapp=баня` | `sauna` |
| 3 | `?startapp=contact` | `contact` |
| 4 | `?startapp=домик` | `booking` |
| 5 | `?startapp=unknown` | `home` |
| 6 | Без онбординга + `?startapp=sauna` | Сначала онбординг → после «Начать» → `sauna` |

---

### ТК-16: Внешние площадки

На экране `booking` — блок «Или забронировать через площадку».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | «ЦИАН» | Открывается `https://www.cian.ru/rent/suburban/325890116` |
| 2 | «Суточно.ру» | Открывается `https://sutochno.ru/front/searchapp/detail/1840600...` |

---

### ТК-17: Webhook (Telegram-бот)

Требуется: Netlify CLI, заполненный `.env`.

```bash
netlify dev
```

```bash
# Команда /start
curl -X POST http://localhost:8888/.netlify/functions/webhook \
  -H "Content-Type: application/json" \
  -d '{"message":{"chat":{"id":12345},"from":{"first_name":"Иван"},"text":"/start"}}'
# Ожидаемый ответ: {"statusCode":200,"body":"OK"}
# Бот отправляет приветствие + клавиатура из 3 WebApp-кнопок
```

| # | Сценарий | Ожидаемый ответ бота |
|---|----------|----------------------|
| 1 | `/start` с именем | «Привет, Иван! 👋» + клавиатура |
| 2 | `/start` без имени | «Привет! 👋» + клавиатура |
| 3 | Любой другой текст | «Воспользуйтесь кнопками ниже» + клавиатура |
| 4 | GET-запрос | `200 OK`, без действий |
| 5 | Пустое тело | `200 OK`, без действий |
| 6 | `TELEGRAM_BOT_TOKEN` не задан | `500 No token` |

---

## 7. Расчёт цен — контрольные значения

Все цены из `data.js`. Уборка в итог **не включена** (убрана из расчёта).

### Домик

| Ночи | Ситуация | Расчёт | Итого |
|------|----------|--------|-------|
| 2 | 2 гостя | 2 × 8 000 | **16 000 ₽** |
| 3 | 2 гостя | 3 × 8 000 | **24 000 ₽** |
| 2 | 5 гостей (баня авто) | 2 × 8 000 + 2 × 3 000 | **22 000 ₽** |
| 3 | 5 гостей (баня авто) | 3 × 8 000 + 3 × 3 000 | **33 000 ₽** |
| 2 | 2 гостя + баня из бани (per-day) | 2 × 8 000 + 2 × 3 000 | **22 000 ₽** |

> `isWeekendDate()` считает пятницу, субботу, воскресенье выходными. Обе ставки (будни/выходные) сейчас одинаковы: 8 000 ₽. Логика разделения в коде есть, цены в `data.js` одинаковые.

### Баня (почасовая, standalone)

| Часов | Расчёт | Итого |
|-------|--------|-------|
| 2 | 2 × 1 500 | **3 000 ₽** |
| 3 | 3 × 1 500 | **4 500 ₽** |
| 4 | 4 × 1 500 | **6 000 ₽** |

### Велосипеды

| Кол-во | Расчёт | Итого |
|--------|--------|-------|
| 1 | 1 × 1 500 | **1 500 ₽** |
| 2 | 2 × 1 500 | **3 000 ₽** |
| 4 | 4 × 1 500 | **6 000 ₽** |

---

## 8. Известные заглушки (не баги)

| Компонент | Текущее значение | Файл:строка |
|-----------|-----------------|-------------|
| Telegram хозяина | `aframe_dom` | `data.js:204` |
| Телефон хозяина | `+79261234567` | `data.js:205` |
| Wi-Fi сеть | `Aframe_Guest` | `data.js → instructions → wifi` |
| Wi-Fi пароль | `nature2024` | `data.js → instructions → wifi` |
| Координаты | `56.3542, 37.5128` | `app.js` экран nearby |
| Расстояние от Москвы | «82 км» в hero, «63 км» в data.js | несоответствие |
| Чип доступности | «Свободно: 26–28 июня» | `app.js` экран home (hardcoded) |
| Галерея | Градиентные заглушки вместо фото | `data.js:56–61` |
| Все submit | Симуляция — заявки не отправляются | `submitBooking / submitSauna / submitBikes` |

---

## 9. Совместимость с Android / Samsung

**Правильный способ открыть на Android:**
1. Найти бота `@repka_domik_bot` в Telegram
2. Нажать «Start» → бот пришлёт клавиатуру с кнопками Mini App
3. Тапнуть кнопку «🏠 Заказать домик» — откроется Mini App внутри Telegram

**Неправильно (SDK не работает):**
- Открыть `https://repka-domik.netlify.app` напрямую — встроенный браузер, без MainButton/BackButton/haptic

| # | Сценарий | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Через бота → кнопку в Telegram Android | Полностью работает, тёмная тема, MainButton видна |
| 2 | Chrome на Android | Браузерный режим, без SDK-функций |
| 3 | Samsung Internet | Работает, blur может быть упрощён |
| 4 | Прямая ссылка в Telegram-браузере | Работает, без MainButton/BackButton/haptic |

| Платформа | Минимальная версия |
|-----------|-------------------|
| Telegram Android | 8.0+ (Bot API 6.0) |
| Android WebView | Chrome 76+ |
| iOS Telegram | 8.0+ |
| Desktop Telegram | Любая современная |

---

## 10. Что тестируется только в реальном Telegram-клиенте

| Функция | Метод SDK |
|---------|-----------|
| MainButton (нижняя кнопка) | `tg.MainButton.show()` |
| BackButton Telegram | `tg.BackButton.show()` |
| Тактильный отклик | `tg.HapticFeedback.impactOccurred()` |
| Открытие Telegram-ссылок | `tg.openTelegramLink()` |
| Имя пользователя в онбординге | `tg.initDataUnsafe.user.first_name` |

---

## 11. Чек-лист перед деплоем

```
[ ] ТК-01: Онбординг — три ✓-строки, кнопка «Начать»
[ ] ТК-02: Таб-бар — все 4 вкладки переключаются
[ ] ТК-03: Главная — цена 8 000 ₽, гости до 6
[ ] ТК-04: Домик — 11 pill-чипов, галерея 5 слайдов
[ ] ТК-05: Бронирование — расчёт без уборки, с баней при 5+ гостях
[ ] ТК-06: Флоу домика — booking → upsellSauna → upsellBikes → success
[ ] ТК-07: Баня к брони — per-day карточка, «Добавить к брони», «✕ убрать»
[ ] ТК-08: Баня почасовая — слоты, длительность, расчёт
[ ] ТК-09: Велосипеды — счётчик, сумма, комментарий, флоу с накопленным заказом
[ ] ТК-10: Что рядом — карта, 4 вкладки
[ ] ТК-11: Инструкции — аккордеон, копирование Wi-Fi
[ ] ТК-12: Контакты — Telegram и телефон хозяина
[ ] ТК-13: Навигация — стек роутера, кнопка «←»
[ ] ТК-14: Оффер-модалка — промокод FIRST10, показывается один раз
[ ] ТК-15: Deep-link ?startapp= — все 4 маршрута
[ ] ТК-17: Webhook — /start и произвольный текст
[ ] Раздел 9: открытие через бота на Android
[ ] Раздел 8: заглушки заменены на реальные данные
```
