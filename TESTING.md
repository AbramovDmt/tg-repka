# TESTING.md — Руководство тестировщика
## Проект: A-frame домик — Telegram Mini App

---

## 1. Что это и как устроено

Telegram Mini App (TMA) для бронирования A-frame домика в Дмитровском районе (Подмосковье).

**Стек:** чистый HTML/CSS/JS — никакого сборщика, никаких зависимостей npm.

```
tg-app/
├── index.html          # Точка входа
├── css/styles.css      # Все стили
└── js/
    ├── data.js         # Весь контент: цены, тексты, даты
    └── app.js          # Логика: роутер, экраны, Telegram SDK

netlify/functions/
└── webhook.js          # Serverless: Telegram-бот webhook

netlify.toml            # Конфигурация Netlify
.env.example            # Шаблон переменных окружения
```

**Production URL:** https://repka-domik.netlify.app  
**Telegram-бот:** @repka_domik_bot

---

## 2. Локальный запуск (без Telegram)

Сборка не нужна. Достаточно открыть файл в браузере.

```
# Вариант А: просто открыть файл
Откройте tg-app/index.html в Chrome/Firefox/Edge

# Вариант Б: через локальный сервер (рекомендуется — clipboard API требует HTTPS/localhost)
cd tg-app
python -m http.server 8080
# затем открыть http://localhost:8080
```

Telegram SDK (`telegram-web-app.js`) подключается из CDN — нужен интернет. Без реального Telegram-клиента SDK отдаёт `null`, и приложение работает в "браузерном режиме":
- `tg` = null → `MainButton`, `BackButton`, `haptic` не вызываются
- Внешние ссылки открываются через `window.open()` вместо `tg.openLink()`
- Тема всегда принудительно тёмная (`forceDarkPalette()` вызывается в любом случае)

---

## 3. Переменные окружения (только для webhook)

Скопируйте `.env.example` в `.env` и заполните:

```
cp .env.example .env
```

| Переменная           | Где взять                      | Используется в          |
|----------------------|--------------------------------|-------------------------|
| `TELEGRAM_BOT_TOKEN` | @BotFather → `/token`          | `netlify/functions/webhook.js` |

Файл `.env` в git не коммитится (есть в `.gitignore`).

Для тестирования бота локально нужен [Netlify CLI](https://docs.netlify.com/cli/get-started/):
```
npm install -g netlify-cli
netlify dev
```

---

## 4. Состояние приложения (localStorage / sessionStorage)

Приложение хранит состояние в браузере. Перед каждым чистым прогоном теста очистите:

| Ключ                  | Хранилище      | Значение                              |
|-----------------------|----------------|---------------------------------------|
| `repka_onboarded`     | localStorage   | `'1'` — пользователь прошёл онбординг |
| `repka_offer_shown`   | localStorage   | `'1'` — модалка скидки уже показана   |
| `repka_pending_route` | sessionStorage | ID экрана из deep-link до онбординга  |

**Сбросить всё:** DevTools → Application → Storage → Clear site data.

---

## 5. Карта экранов

```
onboarding (первый запуск)
    └─ [Начать] ──► home
                        ├─ [info card] ──► house ──► booking ──► success
                        └─ (оффер-модалка, 500 мс после startApp)

Таб-бар (фиксированный снизу):
  🏠 home   →  home
  📅 book   →  booking
  🔥 sauna  →  sauna    ──► success
  ⊞  more   →  more
                   ├── house ──► booking ──► success
                   ├── bikes               ──► success
                   ├── nearby
                   ├── instructions
                   └── contact
```

**Таб-бар скрыт** на экранах, не входящих в основную навигацию: `success`.

---

## 6. Тест-кейсы

### ТК-01: Онбординг (первый запуск)

**Условие:** localStorage пуст.

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть index.html | Экран онбординга: 🏕️, приветствие, блок «Здесь можно:» с тремя ✓-пунктами, кнопка «Начать» |
| 2 | Блок «Здесь можно:» | **Не кликабельный** — это информационный список, не кнопки. Строки с ✓ и серым текстом |
| 3 | Проверить таб-бар | Таб-бар **не виден** (класс `hidden` на `#tab-bar`) |
| 4 | Нажать «Начать» | Переход на `home`, `repka_onboarded = '1'` в localStorage |
| 5 | Подождать 500 мс | Появляется оффер-модалка со скидкой 10% |
| 6 | Повторно открыть | Онбординг не показывается — сразу `home` |

---

### ТК-02: Таб-бар

**Условие:** `repka_onboarded = '1'` (или после прохождения ТК-01).

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Нажать «📅 Бронировать» | Открывается экран `booking`, таб «📅» активен |
| 2 | Нажать «🔥 Баня» | Открывается экран `sauna`, таб «🔥» активен |
| 3 | Нажать «⊞ Ещё» | Открывается экран `more`, таб «⊞» активен |
| 4 | Нажать «🏠 Домик» | Возврат на `home`, таб «🏠» активен |
| 5 | Открыть `house` из `more` | Таб «⊞ Ещё» остаётся активным |
| 6 | Открыть `contact` из `more` | Таб «⊞ Ещё» остаётся активным |

---

### ТК-03: Главная (home)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Просмотреть hero-блок | Отображаются: «Дмитровский район · 82 км от Москвы», «A-frame домик», «Сосновый лес · Баня · Канал» |
| 2 | Проверить info-карточку | Цена: **8 000 ₽/ночь**, Гости: до 4, Площадь: 48 м² · 2 этажа, Заезд с 16:00 / Выезд до 12:00 |
| 3 | Нажать на info-карточку | Переход на экран `house` |
| 4 | Нажать «🔗 Поделиться с другом» | В браузере: `navigator.share` или `clipboard.writeText` с URL `https://t.me/repka_domik_bot` |

---

### ТК-04: Домик (house)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Шапка «Домик», в заголовке — кнопка «←» (назад) |
| 2 | Просмотреть галерею | 5 слайдов: Фасад, Интерьер, Терраса, Баня, Вечер (градиентные заглушки). Точки-индикаторы внизу |
| 3 | Свайп по галерее | Активная точка переключается |
| 4 | Проверить блок удобств | 12 **pill-чипов** (иконка + текст в строку, flex-wrap): Баня-домик, Мангал+казан, Wi-Fi, Бельё и полотенца, Кухня, Тёплый пол, Дрова, Парковка 2 авто, Smart TV, Канал рядом, Питомцы ОК, SUP-борд |
| 5 | Блок «Заезд и выезд» | 🔑 Заезд с 16:00, 🚪 Выезд до 12:00 |
| 6 | Блок «Правила» | 7 правил, включая: тишина после 23:00, залог 5 000 ₽ |
| 7 | Нажать «Проверить даты» | Переход на `booking` |

---

### ТК-05: Бронирование (booking) — Календарь

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Шапка «Бронирование», календарь текущего месяца |
| 2 | Проверить прошедшие даты | Ячейки до сегодня — серые, не кликабельные |
| 3 | Проверить занятые даты | Занятые даты из `APP_DATA.unavailableDates` — отмечены классом `booked`, не кликабельные. Проверьте: `2026-05-23`, `2026-05-24`, `2026-05-25` |
| 4 | Нажать на свободную дату | Дата подсвечивается зелёным (check-in). Блок «Заезд → Выезд» появляется под календарём |
| 5 | Нажать вторую дату позже первой | Диапазон выделяется. Появляется блок с ценой |
| 6 | Проверить расчёт цены | 8 000 ₽ × N ночей + 1 500 ₽ уборка = Итого |
| 7 | Нажать дату до первой | Первая дата сбрасывается, новая становится check-in |
| 8 | Выбрать диапазон через занятую дату | Конфликт: check-in переносится на кликнутую дату, check-out сбрасывается |
| 9 | Нажать «‹» (предыдущий месяц) | Нельзя зайти раньше текущего месяца (кнопка `invisible`) |
| 10 | Нажать «›» (следующий месяц) | Переключается на следующий месяц |
| 11 | Изменить количество гостей | Кнопки «−» / «+», диапазон 1–4, текст обновляется |
| 12 | Написать комментарий | Текст сохраняется в `state.booking.comment` |

**Кнопка Telegram MainButton** (только в Telegram-клиенте):
- Без дат: «Выберите даты» (disabled)
- С датами: «Отправить заявку · N ₽» (enabled)

---

### ТК-06: Бронирование → Успех

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Выбрать даты, нажать MainButton | Переход на `success` с типом `booking` |
| 2 | Экран success | Иконка 🏠, «Заявка отправлена!», «Хозяин свяжется с вами в течение часа...» |
| 3 | Нажать «На главную» | Возврат на `home`, стек роутера сброшен |
| 4 | Таб-бар на success | Таб-бар **скрыт** |

---

### ТК-07: Баня (sauna)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть вкладку «🔥 Баня» | Шапка «Баня», hero-блок: «1 500 ₽/час · до 6 чел. · мин. 2 часа» |
| 2 | Полоса дат (14 дней) | Первая ячейка — «Сег» + дата + месяц |
| 3 | Дата, где все 6 слотов заняты | Ячейка с классом `fully-booked`. Проверьте: `2026-05-23` (`['10:00','12:00','14:00','16:00','18:00','20:00']`) |
| 4 | Выбрать дату `2026-05-21` | Заголовок слотов: «Доступные слоты — 21 мая, чт». Слоты `14:00` и `18:00` — серые (booked) |
| 5 | Нажать свободный слот | Слот выделяется. Появляется блок «дата + время–до + цена» |
| 6 | Изменить длительность (2/3/4 ч) | Цена пересчитывается: 1 500 × N. Время «до» обновляется |
| 7 | Слот 20:00 + 3 ч | «до 22:00» (не уходит за 22:00 по `Math.min`) |
| 8 | Нажать MainButton (с выбранными датой и слотом) | Переход на `success` с типом `sauna`, «Баня забронирована!» |

---

### ТК-08: Велосипеды (bikes)

Доступ: «⊞ Ещё» → «🚴 Велосипеды».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Шапка «Велосипеды», hero: «4 велосипеда · 1 500 ₽/день» |
| 2 | Счётчик велосипедов | По умолчанию: 1. Диапазон: 1–4 |
| 3 | Нажать «+» до 4, затем ещё «+» | Остаётся на 4, не уходит за максимум |
| 4 | Проверить сумму | 1 велосипед → 1 500 ₽, 4 → 6 000 ₽ (1 500 × N) |
| 5 | Блок «Популярные маршруты» | 3 маршрута: Вдоль канала (12 км), До Дмитрова (15 км), Яхрома–Дубна (30 км) |
| 6 | Примечание внизу | «Вернуть велосипеды до 20:00. Выдаёт хозяин при заезде.» |
| 7 | Нажать MainButton «Забронировать» | Переход на `success` с типом `bikes`, «Велосипеды заказаны!» |

---

### ТК-09: Что рядом (nearby)

Доступ: «⊞ Ещё» → «🗺 Что рядом».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Карточка-заглушка карты с «A-frame домик», координаты «56.3542, 37.5128» |
| 2 | Нажать «Открыть в Яндекс.Картах» | Открывается URL `https://yandex.ru/maps/?pt=37.5128,56.3542&z=14` |
| 3 | Вкладки под картой | 4 вкладки: Природа, Активности, Места, Дмитров |
| 4 | Переключить вкладки | Список мест обновляется без перезагрузки страницы |
| 5 | «Природа» → «Канал им. Москвы» | Дистанция: 300 м, время: 4 мин пешком, кнопка «›» |
| 6 | Нажать «›» у места с картой | Открывается Яндекс.Карты с поиском |
| 7 | «Берёзовая роща» | Нет кнопки «›» (поле `mapsUrl: ''`) |

---

### ТК-10: Инструкции (instructions)

Доступ: «⊞ Ещё» → «📋 Инструкции».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | 7 пунктов аккордеона: 🗺 Как добраться, 🔑 Заселение, 📶 Wi-Fi, 🔥 Баня, 🍖 Мангал, 📋 Правила, 📞 Экстренные |
| 2 | Нажать на заголовок | Раскрывается содержимое, стрелка «›» поворачивается |
| 3 | Нажать другой заголовок | Предыдущий закрывается, новый открывается |
| 4 | Нажать тот же заголовок | Закрывается |
| 5 | Раздел «📶 Wi-Fi» → нажать «Скопировать» | Пароль `nature2024` копируется в буфер, кнопка меняется на «Скопировано ✓» (2 сек) |
| 6 | Раздел «🗺 Как добраться» | Ссылка «Открыть в Яндекс.Картах →» (data-action="openLink") |

---

### ТК-11: Контакты (contact)

Доступ: «⊞ Ещё» → «✉️ Связаться с хозяином».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть экран | Аватар «С» (первая буква имени «Сергей»), имя «Сергей», роль «Хозяин домика» |
| 2 | Нажать «✉️ Написать в Telegram» | Открывается `https://t.me/aframe_dom` |
| 3 | Нажать «📞 Позвонить» | Открывается `tel:+79261234567` |
| 4 | Раздел «Частые вопросы» | 5 вопросов-ответов в аккордеоне |
| 5 | Раскрыть «Баня входит в стоимость?» | «Нет, баня бронируется отдельно. 1 500 ₽/час, минимум 2 часа.» |

---

### ТК-12: Ещё (more)

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть вкладку «⊞ Ещё» | 5 пунктов списка: О домике, Велосипеды, Что рядом, Инструкции, Связаться |
| 2 | Нажать каждый пункт | Переход на соответствующий экран, таб «⊞» остаётся активным |
| 3 | Нажать «←» назад из дочернего экрана | Возврат в «Ещё» |

---

### ТК-13: Навигация и кнопка «Назад»

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | home → house → booking | Кнопка «←» появляется в шапке каждого дочернего экрана |
| 2 | Нажать «←» на booking | Возврат на house |
| 3 | Нажать «←» на house | Возврат на home, кнопка «←» исчезает |
| 4 | Переключить таб на «📅» | Стек сбрасывается, кнопки «←» нет |
| 5 | success → «На главную» | Стек сбрасывается, таб «🏠» активен |

---

### ТК-14: Оффер-модалка

Модалка показывается **только один раз** — сразу после первого онбординга (через 500 мс). Повторный показ при возвращении в приложение намеренно отключён.

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Первый запуск → «Начать» → ждать 500 мс | Модалка «🎁 Скидка 10%» появляется с анимацией |
| 2 | Нажать «Получить скидку 10%» | Карточка трансформируется: показывает промокод **FIRST10** в рамке с кнопкой «Скопировать промокод». Редиректа нет |
| 3 | Нажать «Скопировать промокод» | `FIRST10` копируется в буфер. Кнопка меняется на «Скопировано ✓». Через 1.4 сек модалка закрывается |
| 4 | Нажать «Закрыть» | Модалка закрывается, `repka_offer_shown = '1'` в localStorage |
| 5 | Нажать «Пропустить» | Модалка закрывается, `repka_offer_shown = '1'` |
| 6 | Закрыть и открыть снова (onboarded) | Модалка **не появляется** — ни при каком повторном открытии приложения |
| 7 | Сбросить localStorage полностью | Онбординг показывается заново → после «Начать» модалка показывается снова |

---

### ТК-15: Deep-link (startapp-параметр)

URL-параметр `?startapp=` передаётся от Telegram-клиента или вручную в браузере.

| # | URL / параметр | Ожидаемый экран |
|---|----------------|-----------------|
| 1 | `?startapp=booking` или `?startapp=book` | Открывается `booking` |
| 2 | `?startapp=sauna` или `?startapp=баня` | Открывается `sauna` |
| 3 | `?startapp=contact` | Открывается `contact` |
| 4 | `?startapp=домик` | Открывается `booking` |
| 5 | `?startapp=unknown` | Открывается `home` (маршрут не найден) |
| 6 | Без онбординга + `?startapp=sauna` | Сначала онбординг, после «Начать» — `sauna` |

---

### ТК-16: Бронирование через внешние площадки

На экране `booking` — блок «Или забронировать через площадку».

| # | Действие | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Нажать «ЦИАН» | Открывается `https://www.cian.ru/rent/suburban/325890116` |
| 2 | Нажать «Суточно.ру» | Открывается `https://sutochno.ru/front/searchapp/detail/1840600...` |

---

### ТК-17: Webhook (Telegram-бот)

Требуется: Netlify CLI, заполненный `.env`.

```bash
netlify dev
# Function server: http://localhost:8888
```

Отправьте POST-запрос:

```bash
# Команда /start
curl -X POST http://localhost:8888/.netlify/functions/webhook \
  -H "Content-Type: application/json" \
  -d '{"message":{"chat":{"id":12345},"from":{"first_name":"Иван"},"text":"/start"}}'

# Ожидаемый ответ: {"statusCode":200,"body":"OK"}
# Бот должен отправить приветствие с именем и клавиатуру из 3 кнопок-WebApp
```

```bash
# Произвольное сообщение
curl -X POST http://localhost:8888/.netlify/functions/webhook \
  -H "Content-Type: application/json" \
  -d '{"message":{"chat":{"id":12345},"text":"привет"}}'

# Ответ бота: "Воспользуйтесь кнопками ниже или откройте приложение 👇"
```

| # | Сценарий | Ожидаемый ответ бота |
|---|----------|----------------------|
| 1 | `/start` с именем | «Привет, Иван! 👋\nВыберите действие или откройте приложение:» + клавиатура |
| 2 | `/start` без имени | «Привет! 👋\nВыберите действие или откройте приложение:» + клавиатура |
| 3 | Любой другой текст | «Воспользуйтесь кнопками ниже или откройте приложение 👇» + клавиатура |
| 4 | GET-запрос вместо POST | `200 OK` без действий |
| 5 | Пустое тело / не-JSON | `200 OK` без действий |
| 6 | `TELEGRAM_BOT_TOKEN` не задан | `500 No token` |

Клавиатура содержит 3 кнопки WebApp:
- «🏠 Заказать домик» → `https://repka-domik.netlify.app?startapp=booking`
- «🔥 Забронировать баню» → `https://repka-domik.netlify.app?startapp=sauna`
- «✉️ Связаться с хозяином» → `https://repka-domik.netlify.app?startapp=contact`

---

## 7. Расчёт цен — контрольные значения

### Домик

| Ночи | Дни недели | Расчёт | Итого (с уборкой 1 500 ₽) |
|------|-----------|--------|--------------------------|
| 2 | пн–вт | 2 × 8 000 = 16 000 | 17 500 |
| 3 | пт–вс (все выходные) | 3 × 8 000 = 24 000 | 25 500 |
| 5 | пн–пт (смешанные) | 5 × 8 000 = 40 000 | 41 500 |

> Пт, Сб, Вс определяются как `isWeekendDate()`. Сейчас обе ставки одинаковы (8 000 ₽), поэтому все вычисления дают одинаковую ставку.

### Баня

| Часов | Расчёт | Итого |
|-------|--------|-------|
| 2 | 2 × 1 500 | 3 000 ₽ |
| 3 | 3 × 1 500 | 4 500 ₽ |
| 4 | 4 × 1 500 | 6 000 ₽ |

### Велосипеды

| Кол-во | Расчёт | Итого |
|--------|--------|-------|
| 1 | 1 × 1 500 | 1 500 ₽ |
| 2 | 2 × 1 500 | 3 000 ₽ |
| 4 | 4 × 1 500 | 6 000 ₽ |

---

## 8. Известные заглушки (не баги)

Перед полноценным запуском требуют замены на реальные данные:

| Компонент | Текущее значение | Файл |
|-----------|-----------------|------|
| Telegram хозяина | `aframe_dom` | `data.js:204` |
| Телефон хозяина | `+79261234567` | `data.js:205`, `instructions` |
| Wi-Fi сеть | `Aframe_Guest` | `data.js → instructions → wifi` |
| Wi-Fi пароль | `nature2024` | `data.js → instructions → wifi` |
| Координаты | `56.3542, 37.5128` | `app.js:735`, `instructions:140` |
| Галерея | Градиентные заглушки | `data.js:56–61` |
| Чип доступности | «Свободно: 26–28 июня» | `app.js:411` (hardcoded) |
| Бот webhook | Симуляция — данные не отправляются | `app.js → submitBooking/submitSauna/submitBikes` |

---

## 9. Совместимость с Android / Samsung

### Почему приложение может не открыться на Android

Приложение — это **Telegram Mini App**, а не обычный сайт. Оно предназначено для открытия строго через Telegram-клиент, а не через браузер напрямую.

**Правильный способ открыть:**
1. Найти бота `@repka_domik_bot` в Telegram
2. Нажать «Start» — бот пришлёт клавиатуру с кнопками Mini App
3. Тапнуть кнопку «🏠 Заказать домик» — откроется Mini App внутри Telegram

**Неправильные способы (не откроются или откроются неполноценно):**
- Отправить ссылку `https://repka-domik.netlify.app` — Telegram откроет встроенный браузер, а не Mini App. Приложение запустится, но без SDK (MainButton, BackButton, haptic не работают)
- Отправить ссылку `https://t.me/repka_domik_bot` — откроется чат с ботом, Mini App нужно запускать отдельно через кнопку

### Что видит пользователь при открытии через встроенный браузер на Samsung

Telegram на Android отображает встроенный браузер с заголовком «Домик Репка». При этом возможны:
- Белый экран с иконкой загрузки — страница загружается, но медленно
- Тёмный экран без содержимого — SDK = null, стили применились, но `DOMContentLoaded` ещё не отработал
- Частичное отображение без анимаций — `backdrop-filter: blur()` не поддерживается в старых WebView

### Проверка совместимости

| # | Сценарий | Ожидаемый результат |
|---|----------|---------------------|
| 1 | Открыть через бота → кнопку в Telegram (Android) | Полностью работает, тёмная тема, MainButton видна |
| 2 | Открыть `https://repka-domik.netlify.app` в Chrome на Android | Работает в браузерном режиме, без SDK-функций |
| 3 | Открыть `https://repka-domik.netlify.app` в Samsung Internet | Работает, анимации и blur могут быть упрощены |
| 4 | Открыть прямую ссылку в Telegram (встроенный браузер) | Работает, без MainButton/BackButton/haptic |

### Минимальные требования

| Платформа | Минимальная версия |
|-----------|-------------------|
| Telegram Android | 8.0+ (Bot API 6.0) |
| Android WebView | Chrome 76+ (поддержка backdrop-filter) |
| iOS Telegram | 8.0+ |
| Desktop Telegram | Любая современная |

---

## 10. Что тестируется только в реальном Telegram-клиенте

Следующие функции недоступны в браузере:

| Функция | Метод SDK |
|---------|-----------|
| MainButton (нижняя кнопка) | `tg.MainButton.show()` |
| BackButton Telegram | `tg.BackButton.show()` |
| Тактильный отклик | `tg.HapticFeedback.impactOccurred()` |
| Открытие Telegram-ссылок | `tg.openTelegramLink()` |
| Имя пользователя в онбординге | `tg.initDataUnsafe.user.first_name` |
| Принудительная тёмная тема | `forceDarkPalette()` — вызывается, но SDK отсутствует |

Для тестирования в Telegram: опубликуйте `tg-app/` на любой HTTPS-хостинг (Netlify, Vercel, GitHub Pages) и настройте Mini App в @BotFather командой `/newapp`.

---

## 11. Чек-лист перед деплоем

```
[ ] ТК-01: Онбординг — блок «Здесь можно:» с ✓, кнопка «Начать»
[ ] ТК-02: Таб-бар — все 4 вкладки
[ ] ТК-03: Главная — цена и карточка
[ ] ТК-04: Домик — pill-чипы удобств, галерея, правила
[ ] ТК-05: Бронирование — выбор дат и расчёт
[ ] ТК-06: Успех — переход и возврат
[ ] ТК-07: Баня — слоты и длительность
[ ] ТК-08: Велосипеды — счётчик и сумма
[ ] ТК-09: Что рядом — все 4 вкладки
[ ] ТК-10: Инструкции — аккордеон и копирование Wi-Fi
[ ] ТК-11: Контакты — ссылки на Telegram и телефон
[ ] ТК-13: Кнопка «Назад» — стек роутера
[ ] ТК-14: Оффер-модалка — показывается один раз, промокод FIRST10 копируется, петли нет
[ ] ТК-15: Deep-link ?startapp= — все маршруты
[ ] ТК-17: Webhook — /start и произвольный текст
[ ] Раздел 9: открытие через бота на Android работает
[ ] Заглушки заменены на реальные данные (раздел 8)
```
